# =============================================================
# Test fixture: .properties secrets detection
# =============================================================
# Lines marked DETECT should be flagged by Semgrep rules.
# Lines marked SKIP should NOT be flagged.

# --- DETECT: hardcoded passwords ---
db.password=SuperSecret123
spring.datasource.password=plaintext_pass
redis.password=r3d1s_s3cr3t
ci.serviceAccountAdminPassword=admin123
pinot.password=ENC(JMSaitq4mmKs2oACUWSje3O5fzg)

# --- DETECT: hardcoded API keys ---
api_key=sk_live_abc123def456
auth_token=eyJhbGciOiJIUzI1NiJ9.test

# --- SKIP: Spring Boot ${VAR} placeholders ---
db.password=${DB_PASSWORD}
spring.datasource.password=${DATASOURCE_PASSWORD:}
ci.serviceAccountAdminPassword=${CI_SERVICE_ACCOUNT_ADMIN_PASSWORD:}
redis.password=${REDIS_PASSWORD}

# --- SKIP: Helm/Vault {{ }} templates ---
db.password={{vault.db.password}}
spring.datasource.password={{ .Values.password }}

# --- SKIP: commented out lines ---
#db.password=old_password_123
# password=commented_out_secret

# --- SKIP: closing brace from ${VAR_PASSWORD:} ---
# This line simulates the regex matching PASSWORD:} inside a placeholder
# The actual ${...} pattern is tested above
